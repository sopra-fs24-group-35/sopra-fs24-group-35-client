export type User = {
  avatarId: any;
  username: string;
  name: string;
  id: number;
};

export type Player = {
  username: string;
  riskCards: any;
  id: number;
};

export type Card = {
  troops: number;
  territoryName: string;
};
