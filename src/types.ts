export type User = {
  avatarId: any;
  username: string;
  name: string;
  id: number;
};

export type Player = {
  username: string;
  riskCards: any;
  id: number;
};

export type Card = {
  troops: string;
  territoryName: string;
  id: number;
};
